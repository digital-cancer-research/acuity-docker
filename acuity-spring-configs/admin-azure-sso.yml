# data of AdminUI application registration on Azure portal; `resource` and `client` same-named options has identical values
azure:
  resource:
    clientId:
    clientSecret:
  client:
    clientId:
    clientSecret:
